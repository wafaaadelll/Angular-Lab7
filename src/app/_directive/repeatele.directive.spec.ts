import { RepeateleDirective } from './repeatele.directive';

describe('RepeateleDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeateleDirective();
    expect(directive).toBeTruthy();
  });
});
